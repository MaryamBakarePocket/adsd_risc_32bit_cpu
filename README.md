# adsd_risc_32bit_cpu
This repository contains the system verilog files of a 32bit cpu which includes a top module and its submodules which are - ALU, RAM(Data Memory), ROM (Instruction Memory), Clock divider, Register, Program Counter and each submodule and top module testbenchesbench
